# Windows 新手版 0.6.0

**本版统一从“启动论文助手.bat”进入。** 首页可以新建普通论文或 AI 教学练习，也可导入旧版论文。论文与程序分开保存，首页显示实际目录。新增每日启动备份、项目 ZIP 导出、查找替换和插入预览，请先阅读 [v0.6 图文使用与迁移指南](docs/UPGRADE_0_6.md)。下方保留各功能的操作说明；涉及旧版路径时以新指南为准。

**0.5.0：连续 PDF 阅读与双向定位。**左栏编辑器铺满可用空间，右栏可滚动整篇 PDF，支持 25%–300% 和自定义缩放。双击 Markdown 或 PDF 正文可定位到对应内容块。升级后请先编译一次。详见 [左右对照预览说明](左右对照预览说明.md)。

**0.4.8 新增左右对照 PDF 预览。**进入“写论文”，左侧编辑、右侧查看真实 PDF；停止输入 3 秒后自动编译，也可手动编译。详细步骤见 [左右对照预览说明](左右对照预览说明.md)。

先学习完整示例：双击 **启动AI示例教学.bat**，按页面“AI 示例教学”完成九项练习。它使用 GitHub 原 STM32 智能照明 AI 论文的 Markdown 教学改编，不覆盖普通论文。详细说明见 [AI示例教学手册](AI示例教学手册.md)。正式写自己的内容仍用 **启动论文助手.bat**。

## 章节多于或少于五章怎么办？

五章只是示例，不是模板限制。进入“写论文”，点击“管理章节（增减 / 排序）”：

- **新增一章**：填章节名称，例如“实验结果与分析”。文件与配置自动创建，新章位于正文末尾。
- **改名**：修改章节标题，保留原正文和已有标题标签。
- **上移 / 下移**：调整编排顺序，不需要手动修改文件名或编号。
- **移出正文（保留文件）**：不再参与 PDF 编译，文件和正文仍然保留。
- **恢复到正文末尾**：从管理窗口下方的已移出列表恢复，再用上移调整位置。

至少保留一章。每次增减、改名和排序后重新生成 PDF，目录和章节编号随之更新。不要手动在章节标题里填写“第六章”等编号。
若其他章节引用了被移出章节中的图、表或公式，需修改这些引用后重新编译。摘要、总结、谢辞、附录是独立部分，不在本次“正文章节”管理范围内。

## 开始使用

1. 将整个 ZIP 解压到短路径，例如 `D:\XIT-Thesis`。不要在压缩软件中运行，不要只拖出 BAT。
2. 双击根目录的 `启动论文助手.bat`。运行时保留命令窗口。
3. 先点“检查与导出 → 生成 PDF”，确认示例在你的电脑上能编译，再开始写作。
4. “填写资料”修改姓名、学号、题目；停止输入约 1.6 秒保存。
5. “写论文”选择章节，直接写段落。停止输入约 1.2 秒保存，也可以 Ctrl+S。显示未保存或保存错误时，不要关闭窗口。
6. “三线表”中粘贴从 Excel 复制的矩形区域（第一行列名）；公式表单只填数学表达式，不必编写编号；图片按钮选择文件并填标题。编号标签自动生成。
7. “文献引用”可以选择已有文献、填写真实文献信息，或导入 UTF-8 编码的 `.bib` 文件。导入发生重复代号或格式错误时会拒绝整批导入，原库保留。文献来源和内容真实性仍由作者核实。
8. 点击“历史版本 / 恢复”，选择正文或基本资料，再选择时间并预览。恢复前也保留当前版本。每个文件保留最近 100 次实际变化前的版本，存放在 `markdown/thesis/.history`。这不能替代异地备份。
9. 生成失败时，先处理可点击的问题卡片；点击会跳到对应章节行。其他编译问题显示处理提示和原始日志。
10. 题目超出原封面栏宽时停止编译。请缩短题目或请维护者按学院要求修改封面；不会自动缩小字号。

## 包内环境与限制

Windows x86-64 包内置独立 Python 3.12.7、PyYAML 6.0.2、BibTeXParser 1.4.3、PyParsing 3.2.3、Pandoc 3.11 和本次使用的 TinyTeX/TeX Live 2026 环境。启动器只修改自身进程环境，不改系统 PATH，不需要先装 Python、Pandoc 或 TeX。

字体遵循原模板选择规则。没有打包 Windows 商业字体；仍使用本机 Windows 字体。缺失字体、自行加入的新宏包、特殊数学命令及其他电脑差异可能导致编译失败。包内环境通过本机隔离 PATH 测试不等于所有 Windows 电脑均已验证。

正文编辑仍是 Markdown，公式仍使用 LaTeX 数学语法。编译成功不代表符合当年所有毕业论文要求，正式提交前务必逐页核查 PDF。

升级时先复制旧包的 `markdown/thesis` 到安全位置，再将其复制到新包相同位置。不要覆盖原有论文后才备份。建议只打开一个助手页面编辑同一项目。

新增：写论文 → 整篇备份 / 回退；以及“大字阅读”。详见 [备份与恢复说明](备份与恢复说明.md)。

新增：写论文页面的撤销 / 重做按钮及 Ctrl+Z、Ctrl+Y、Ctrl+Shift+Z。详见 [撤销重做说明](撤销重做说明.md)。

新增：浏览器本地草稿保护和重开恢复提示。完整包支持本地离线写作，条件见 [自动保存与离线使用](自动保存与离线使用.md)。

新增：启动前检查、运行诊断、随包字体预览、联网补装宏包。详见 [Windows兼容与环境修复](Windows兼容与环境修复.md)。
