function renderTutorial(mode) {
  const teaching = mode === 'ai';
  document.querySelectorAll('.ai-only').forEach(item => item.classList.toggle('hidden', !teaching));
  if (!teaching) return;
  document.title = 'AI 示例教学 · 毕业论文助手';
  document.querySelector('.brand h1').textContent = 'AI 示例论文教学助手';
  const target = document.querySelector('#lessonCards');
  if (target.children.length) return;
  const lessons = [
    ['1. 先生成整篇论文', '点击生成 PDF。找到封面、摘要、目录、五章正文、参考文献和两个附录。原稿与 Markdown 版可能分页不同，不以页数完全相同作为通过标准。', null, 'build'],
    ['2. 修改封面信息', '把示例作者“张三”改成“练习学生”，等自动保存后重新生成 PDF，核对封面变化。姓名、学号和教师均为示例信息。', null, 'metadata'],
    ['3. 修改一段正文', '打开绪论，在“研究背景与意义”末尾增加一句自己的练习文字。输入停止后自动保存；可通过历史版本恢复。章数变更后，“全文共分为五章”这类叙述需自行改写。', 'chapters/chapter1.md'],
    ['4. 看懂图片与表格', '第二章有界面原型图、系统架构图和性能指标表。先改一处图题，再用“三线表”粘贴 Excel 的两行两列练习数据。新增表会自动生成独立标签。', 'chapters/chapter2.md'],
    ['5. 练习编号公式', '第三章有电流换算公式。用“编号公式”插入 y = ax + b，生成 PDF 查看编号。数学表达式仍需使用 LaTeX 语法，不能把练习公式当作本系统实际算法。', 'chapters/chapter3.md'],
    ['6. 核对模拟数据', '第五章展示光照误差、响应时间和能耗对比。计算 (12.6−7.4)/12.6×100%，约为 41.3%。这只能检查模拟数字的计算一致性，不能证明真实节能效果。', 'chapters/chapter5.md'],
    ['7. 练习文献引用', '在绪论中点击“文献引用”，选择已有文献后生成 PDF，核对编号。原库含“某某大学”的演示占位条目；所有文献都需自行核验。演示文献不等于已验证依据。', 'chapters/chapter1.md'],
    ['8. 练习章节增减与恢复', '点击“管理章节”，新增“拓展实验”，上移，再移出并恢复。重新生成 PDF 检查目录；其他章节对已移出图表的引用也需调整。', 'chapters/chapter5.md'],
    ['9. 查看代码附录', '附录一保留 C 代码排版示例。只用于观察代码块格式，未做硬件验证；不能据此宣称硬件工程已运行。', 'appendices/appendixA.md'],
  ];
  const walkthroughs = [
    ['关闭其他论文助手，确认页眉写着“AI 示例论文教学助手”。先不要修改任何文字。', '点下方按钮进入“检查与导出”，点击“生成 PDF”，保持启动黑窗口打开，等待处理结束。', '成功后点击“打开生成的 PDF”。依次查看封面、摘要、目录和正文；点击目录标题，看看能否跳到相应页面。', '如果失败，先看页面给出的具体文件与行号。程序未找到时回“开始”刷新状态，确认完整解压了 runtime；不要连续重复点击编译。'],
    ['点下方按钮进入“填写资料”，找到姓名字段，将“张三”改为“练习学生”。本次只改姓名，其他字段先不动。', '停止输入后等待页眉显示“内容已同步”。如果提示保存失败，先解决提示，不要关闭页面。', '点左侧“检查与导出”重新生成 PDF，再打开新结果；已经打开的旧 PDF 标签页需要刷新。', '封面应出现“练习学生”。随后可同样修改学号、专业和指导教师；示例信息不能用于正式提交。'],
    ['点下方按钮打开“绪论”。# 开头的是章标题，## 是节标题，### 是小节标题；正文直接输入，不要给普通段落加 #。', '在第一段末尾另起一段，输入“这是一段用于学习排版的练习文字。”，按 Ctrl+S 保存。', '重新生成 PDF，在绪论中找到刚输入的句子。源文件中的空行用来分段，PDF 的字体和段落样式由模板设置。', '想撤销已保存的练习时，点“历史版本 / 恢复”，选择绪论和修改前的版本。恢复会改变当前内容，操作前确认选对文件与时间。'],
    ['进入第二章，找到 ![OLED/上位机监测界面原型图]。只改方括号内的图题，例如末尾加“（教学练习）”；圆括号里的路径和花括号里的标签先不要改。', '将光标放在完整段落后面的空行，点击“三线表”。标题填写“练习测量记录”。', '在 Excel 中准备两列：第一行“次数”和“读数”，第二行“1”和“10”。选中这四个单元格复制，粘贴到表格窗口，再点“确定”。不要用空格代替 Excel 的列分隔符。', '生成 PDF，核对新图题、新表题、三条横线及随表插入的引用。若提示列数不一致，重新复制完整矩形区域。想加新图片时在空行使用“插入图片”，它会新增图片，不会自动替换旧图。'],
    ['进入第三章，把光标放在一段正文之后的空行，避免插入已有公式、表格或代码块内部。', '点击“编号公式”，保留 y = ax + b，直接点“确定”；输入框内不用额外写 $$。', '页面会插入公式块和“由式可知……”引用句。把省略号改为“这是用于学习编号的示例表达式。”，保留自动生成的标签和链接。', '生成 PDF，确认公式有编号、引用没有问号。出错时通过历史恢复本次插入，再使用最简单的 y = ax + b 重试。'],
    ['打开第五章，找到模拟能耗对比表，记录常亮方案 12.6 kWh 和智能控制方案 7.4 kWh。', '用计算器输入 (12.6 - 7.4) / 12.6 * 100，结果约为 41.3%。对照正文中的百分比是否一致。', '先不改这些数值。若练习修改数据，要同时检查正文、表格和图片：本项目中的曲线图片不会因修改表格自动重画。', '这里练习的是数字核对，不是进行真实实验。第五章保留的 AdamW 公式只展示多行公式排版，与照明控制算法无关。'],
    ['先在绪论某个完整句子末尾点击，放置光标；然后点击工具栏“文献引用”，不用先编辑 references.bib。', '在“已有文献代号”下拉框中选择一条已有条目，再点“确定”。正文将出现 [@代号]，它是引用标记，生成 PDF 后才显示为文献编号。', '重新生成 PDF，检查正文引用和末尾参考文献。文献库中有条目并不代表它一定会出现在参考文献列表中，要看正文是否引用及模板设置。', '有真实来源时，可在同一下拉框选择“＋ 填写新文献”逐项填写，或“＋ 导入 BibTeX 文件”选择 .bib，导入后再选条目引用。不要编造作者、年份和 DOI。', '左侧“参考文献库”用于查看原始条目，通常不必手动改。@ 后面是类型，花括号后的第一个名称是代号；改代号后旧的正文引用也必须同步修改。原库含“某某大学”占位条目，仅供练习。'],
    ['点下方按钮后，再点“管理章节（增减 / 排序）”。点击“＋ 新增一章”，名称填“拓展实验”，不用写“第六章”。', '新增后点“完成”，在新章标题下面写一句练习正文，再保存。重新生成 PDF，目录应多出这一章。', '再次打开章节管理，找到“拓展实验”，点“上移”。重新生成 PDF，检查顺序和编号变化；不要手动给标题编号。', '点“移出正文（保留文件）”，该章不再参与编译；在窗口下方可点“恢复到正文末尾”。至少要保留一章。', '五章不是限制。章节增减后，正文中“全文共分为五章”等文字不会自动改写；移出某章后，也要处理其他章节对其图表的引用。'],
    ['打开附录一，观察代码前后各有一行三个反引号；它们围住的内容会按代码块排版。', '第一次只修改代码块以外的一句解释文字，不改代码或反引号。保存并生成 PDF，检查附录中的解释是否更新。', '练习完成后关闭教学助手，双击普通“启动论文助手.bat”开始自己的论文。两种入口编辑不同目录，教学内容不会自动变成正式论文。', '需要保留练习时，关闭助手后复制整个 XIT 文件夹作为备份；不要覆盖自己已有的论文。历史版本是辅助恢复，不能代替独立备份。']
  ];
  lessons.forEach(([title, description, path, panel], index) => {
    const card = document.createElement('article'); card.className = 'action-card';
    const heading = document.createElement('h3'); heading.textContent = title;
    const text = document.createElement('p'); text.textContent = description;
    const steps = document.createElement('ol'); steps.className = 'lesson-steps';
    walkthroughs[index].forEach(step => { const item = document.createElement('li'); item.textContent = step; steps.append(item); });
    const result = document.createElement('p'); result.className = 'lesson-result';
    result.textContent = '每次只做一项修改 → 等待“内容已同步” → 重新生成 PDF → 对照本项要求检查。完成后点击左侧“AI 示例教学”返回这里，继续下一项。';
    const button = document.createElement('button'); button.className = 'button secondary'; button.textContent = '开始这一项练习';
    button.onclick = async () => {
      if (!(await flushSaves())) return;
      if (path) {
        const file = state.project.files.find(item => item.path === path);
        if (!file) return notice('该示例章节已移出正文，请先在章节管理中恢复。', true);
        await loadFile(file);
      }
      showPanel(panel || 'writing');
    };
    card.append(heading, text, steps, button, result); target.append(card);
  });
}
