This document validates a workflow in which Markdown is used as the thesis content source and the existing LaTeX template remains the typesetting backend. Text, equations, tables, code, and references are maintained as plain text, while the build script generates LaTeX and PDF in a temporary directory. This example demonstrates the authoring and build process only and must not be submitted as research work.
