# 使用方式与边界

## 适合直接使用 Markdown 的内容

正文段落、各级标题、列表、数学公式、普通三线表、代码块和引用都可以直接维护为 Markdown。

## 需要保留 LaTeX 逃生通道的情况

复杂宽表、特殊数学环境或学院临时增加的格式要求可能超出通用 Markdown 的表达能力。本项目保留 Pandoc 的 raw LaTeX 支持，因此必要时可以在局部插入少量 LaTeX，而不需要把整篇论文退回到手写 LaTeX。
