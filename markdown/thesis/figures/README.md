# figures

把论文图片放在这里，并在 Markdown 中使用相对于 `thesis/` 的路径，例如：

```markdown
![系统总体架构](figures/system-architecture.png){#fig:system width=96%}
```

正文引用：

```markdown
系统总体架构如[图](#fig:system)所示。
```

建议文件名只使用简短英文、数字、连字符或下划线，并把图片文件一并提交到 Git。
