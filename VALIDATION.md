# 验证记录

## v0.6.0（2026-09-19）

- 35 项 Python 测试通过；新增项目副本隔离、固定模板校验、项目 ZIP 重新读取、每日备份恢复、预览过期检测。恢复整篇备份的 HTTP 测试同时检查旧 PDF 与定位缓存被清除。
- JavaScript 撤销和浏览器草稿测试通过；新增前端脚本通过 Node 语法检查。
- 使用独立测试项目真实编译 AI 示例：29 页；PDF SHA-256 与定位映射相符，预览状态为 current。
- 严格检查加原模板字体的定稿导出通过；预览、定稿导出和项目 ZIP 导出前后，论文源文件哈希一致。
- 项目导出 ZIP 的结构、原模板字节内容和 CRC 检查通过。
- 将完整 ZIP 解压至 D 盘中文目录，仅使用包内嵌入式 Python 启动：内置工具检查、新建 AI 项目、完整预览编译及重新打开运行中项目均通过。项目握手显式绕过系统代理，只访问本机回环地址。
- 浏览器实际检查首页、公式预览、Excel 表格预览、查找全部替换与撤销恢复原文；采集 3 张新版教程截图。
- 测试采用本机 Windows 与已有 TeX Live 2026 环境，不代表所有 Windows/ARM/机房环境。没有断电、磁盘损坏、海量论文或长时间并发压力测试。

## v0.5 发布整理验证（2026-09-19）

本次从用户提供的 v0.5.0 ZIP 提取源码，重写仓库首页、新手图文指南、常见问题和开发说明，采集 8 张实际界面截图。运行环境保持在完整 ZIP 中，不提交 Git。

- 32 项 Python 测试通过，无失败、无跳过。
- JavaScript 撤销与本地草稿两组测试通过。
- 普通模板的 Markdown 检查和 LaTeX 工程生成通过。
- 为源码测试加入统一入口；宏包安装测试使用临时工具路径，不依赖完整运行包。
- CI 指定 Pandoc 3.11，并配置源码测试入口。尚需以 GitHub Actions 实际运行结果确认云端环境。
- 此次主要改变发布文档与测试配置，没有修改助手应用或论文编译实现。

## v0.5.0 既有功能验证

- AI 示例完整编译 29 页，158 个内容块定位标记全部在 PDF 中找到。
- 与 v0.4.9 验证 PDF 逐页比较：文字内容一致，文字边界坐标最大差异小于 0.02 PDF 点。
- 浏览器实测左栏铺满、25%/300% 缩放、连续滚至末页、双向段落定位、跨页代码定位、未编译内容拒绝定位。
- 中文目录下普通论文的预览 API 完整编译成功；PDF 与定位数据哈希匹配，源文件前后哈希一致。
- 已验证服务停止后的浏览器草稿恢复；没有进行断电、磁盘故障或所有浏览器隐私设置的测试。

定位为段落或内容块级，不保证封面、自动生成的目录和文献、特殊自定义 LaTeX 的逐字对应。未覆盖所有 Windows 设备、ARM 实机或机房权限环境。替代字体编译与原字体定稿不能视为完全一致。
