# 0.5.0 连续 PDF 与双向定位

- 左栏铺满可用区域，右栏连续滚动、按需绘制和画布释放。
- 25%–300%、13 个固定档位、整页/宽度适配、自定义比例与加减按钮。
- 通过 PDF 位置标记进行内容块级双向跳转；校验 PDF 与源文件哈希，拒绝过期定位。
- 支持跨章节、图表、公式和跨页代码定位；保留自动保存、撤销和草稿恢复。

# 0.4.9 中文路径修复

- 原位置保留论文，临时英文目录编译，兼容随包 TeX 的中文路径问题。
- 显式格式文件路径，复用 Biber 临时缓存，预览完整日志持续保留。

# 0.4.8 左右对照预览

- 写论文视图左右分栏，停止输入 3 秒后自动编译，支持页码与缩放。
- 隔离快照构建，编译期间继续编辑，后续修改排队更新。
- 编译失败保留上次成功 PDF，离线 PDF.js 6.3.289。

# 0.4.7 环境适配

- 启动预检查、环境诊断、开源字体回退和预览入口、随包 tlmgr 宏包安装入口。
- 补充字体许可资料和兼容性边界。

# 0.4.6 自动草稿与重开恢复

- 输入即时浏览器草稿、成功保存后按内容清理、恢复预览与副本下载、基本资料保护、存储失败提示。
- 默认按项目路径固定本机端口，支持下次启动访问相同浏览器存储。

# 0.4.5 撤销与重做

- 每文件 100 步当前会话编辑历史、连续输入合并、快捷键与禁用态按钮、辅助插入撤销、自动保存。

# 0.4.4 整篇备份与便捷操作

- 命名整篇备份、恢复预览、恢复前自动保护、损坏与过期检查、旧页面会话失效。
- 大字阅读开关，只影响编辑显示。

# 0.4.3 新手教学与编辑布局

- 修复参考文献模式隐藏工具栏后编辑区变矮的问题；最小高度 480 像素，支持纵向拖动。
- 九项教学增加逐步操作、输入示例、检查方法及常见问题处理，同步到教学手册。

# Changelog

## 0.3.3 - Windows 内置 Pandoc

- Windows 完整包附带未修改的 Pandoc 3.11 官方便携版及原始版权和许可文件。
- 启动器、界面环境检查与构建脚本自动识别 runtime/pandoc/pandoc.exe，无需系统 PATH。
- 教程更新为完整解压、双击启动、填写与编译的流程。

## 0.3.2 - 多 Python 环境启动修复

- 逐个检查 PATH 中的 Python，跳过低于 3.10 的版本，解决 Python 3.8 排在 Anaconda Python 3.12 前面时错误提示“未安装”的问题。
- 使用带引号的完整解释器路径，支持安装路径包含空格。

## 0.3.1 - Windows 启动修复

- 启动 BAT 改用纯 ASCII 提示，避免 Windows 批处理代码页切换造成乱码和命令截断；浏览器界面仍为中文。
- 打包时统一 BAT 的 CRLF 换行，防止编辑器引入 LF 换行。
- 实际检查 Python 版本，优先使用 python，失败时尝试 py -3。

## 0.3.0 - beginner interface

- 新增根目录“启动论文助手.bat”，双击后自动打开本地浏览器界面。
- 新增可视化基本信息表单，无需手动编辑 YAML。
- 新增摘要、章节、总结、谢辞、附录和参考文献的统一编辑器。
- 新增标题、公式、表格、引用和代码的快捷插入按钮。
- 新增图片上传与 Markdown 图片语法自动生成。
- 新增环境状态面板，以及检查、生成 PDF、定稿检查和打开 PDF 按钮。
- 所有写入限制在 metadata.yaml、配置列出的正文文件和 figures/ 图片目录。
- 命令行、批处理脚本和原构建程序继续保留，供高级用户和自动化使用。

## 0.2.1 - 2026-09-17

- 修复中文题目和自定义 Logo 替换误命中模板注释示例的问题。

- 修复 Pandoc 新版表格标签丢失，恢复表格交叉引用。
- 修复代码块标签被结构检查器忽略的问题。
- 修复 Lua 标题转义中反斜杠被二次转义的问题。
- 分离 Pandoc 标准输出和错误输出，避免警告混入 LaTeX 源码。
- 使用兼容 Windows 常见终端编码的状态提示。
- 阻止构建清理覆盖论文源目录或模板输入。
- 调整示例长行，补充环境安装和在线平台使用说明。
- 本机完成 10 项测试及 XeLaTeX → Biber → XeLaTeX × 2 的完整 PDF 构建。

## 0.2.0 - beginner-first upgrade

- 新增 `xit.py` 简化入口：`doctor / setup / check / tex / pdf / final-check`。
- 新增 Windows 双击脚本和 macOS/Linux shell 入口。
- 新增无第三方依赖的 `tools/doctor.py` 环境自检。
- 新增交互式 `tools/setup_metadata.py`，不熟悉 YAML 也能填写基本信息。
- 构建输出改为中文分步提示，并优先展示第一条关键 LaTeX 错误。
- 图片、交叉引用、文献键、重复标签错误增加文件名与行号。
- 新增未填写占位字段提醒与 `--strict` 定稿检查。
- 新增 `build/build-report.txt` 构建报告。
- 新增最终 LaTeX 日志扫描，提醒未解析引用和 Overfull 行。
- 将长文档拆分为新手教程、写作速查、故障排查、定稿检查、Git/AI 工作流。
- 新增 VS Code 基础设置和可选扩展推荐。
- 自动测试从 5 项增加到 7 项。

## 0.1.0

- 初始 Markdown/YAML/BibTeX -> Pandoc -> 原 LaTeX 模板 -> PDF 工作流。
# 0.4.0

- Windows 随包独立 Python、Python 依赖、Pandoc、TinyTeX，进程内配置路径。
- 停止输入后自动保存；原子替换文件；最近 100 次历史与恢复；正文保存冲突拦截。
- 三线表粘贴表单、编号公式表单、唯一对象标签、上传图片标题表单及安全文件名。
- 文献填写、选择引用和 BibTeX 文件导入，重复代号整批拒绝。
- 常见写作问题支持点击定位；翻译部分编译错误；失败时隐藏旧 PDF 入口。
- 按原模板实际黑体三号测量中文题目，超宽时中止，保留原模板字号与版式。
# 0.4.1

- 新增正文章节管理：新增、改名、上移/下移、移出与恢复，至少保留一章。
- 显示章节实际名称，编号由列表顺序和模板生成；移出操作保留原文件。
- 检测章节管理页面的过期数据，防止旧页面覆盖刚修改的章节。
- ZIP 内顶层目录缩短为 XIT，降低 Windows 解压路径过长的概率；仍建议解压到盘符下的短路径。
# 0.4.2

- 接入 GitHub 原 STM32 智能照明 AI 示例，提供独立启动、独立论文文件与独立编译输出。
- 页面提供九项教学练习、原始 PDF 对照与模拟内容提示。
- 保留来源 ZIP 与 Git blob/SHA256 记录，记录界面图片路径等适配差异。
- 修复 listings 语言方括号转义，以及分章转换时同名小节自动标签重复；显式图表标签继续保留。
