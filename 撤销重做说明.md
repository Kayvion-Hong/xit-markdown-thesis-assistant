# 撤销与重做：像常用文字软件一样编辑

在“写论文”页面使用“↶ 撤销”“↷ 重做”。灰色按钮表示没有可撤销或可重做的步骤。

- 撤销：编辑框内按 Ctrl+Z。
- 重做：编辑框内按 Ctrl+Y 或 Ctrl+Shift+Z。
- 连续输入间隔小于约 1 秒且光标连续时合并为一步；停顿后开始新一步。
- 粘贴、中文输入法一次提交和辅助工具插入按独立步骤记录。
- 撤销后再输入新内容，会清空旧的重做分支。
- 切换章节，各文件在本页面内保留独立记录，每个文件最多 100 步。
- 撤销或重做后会自动保存；生成 PDF 前等待“内容已同步”。PDF 不会随编辑即时更新，需要重新生成。

练习：在摘要末尾输入 TEST，按 Ctrl+Z 撤回，再按 Ctrl+Y 恢复，最后再撤销并保存，原文保持不变。

这些操作针对当前正文或文献编辑框，不会撤销章节新增、排序、元数据表单、磁盘图片上传或文献库导入等外部操作。撤销插入图片的 Markdown 不会删除图片文件；撤销引用不删除文献库条目。

刷新或关闭页面后，编辑撤销记录消失。外部程序或历史恢复改变文件内容后，重新载入会重置该文件的撤销记录，避免覆盖新版本。需要跨会话恢复时使用“历史版本 / 恢复”或“整篇备份 / 回退”。

快捷键在正文编辑框聚焦时生效。资料填写框保留浏览器自带的输入操作。这是接近 Word 的撤销交互，并非完整 Word 编辑器。
