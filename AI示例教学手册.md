# 用 GitHub 原 AI 示例练习论文排版

第一次练习请先打开 **[AI 示例图文实操](docs/AI_WALKTHROUGH.md)**。它按操作顺序配图，写明每一步点击哪里、修改什么，以及如何检查结果。本页保留示例来源、转换说明和页面内九项练习的文字参考。

![教学项目的分步练习入口](docs/images/07-ai-tutorial.png)

写作界面支持连续 PDF 阅读、25% 至 300% 及自定义缩放，也可双击正文在 Markdown 与 PDF 内容块之间定位。使用前先成功编译一次，详见 [左右对照预览说明](左右对照预览说明.md)。

本教学项目改编自原模板公开仓库中的《基于 STM32 的智能照明系统设计》。
原始来源：[GitHub 项目](https://github.com/Kayvion-Hong/xit-bachelor-thesis-latex-template)。
2026-09-18 核对的 AI ZIP Git blob 为 `72657421df9eeb42816752c5cf7c0301aea2eb9a`；本地原包与 GitHub 一致。

## 先打开正确入口

1. 完整解压到短路径，例如 `D:\XIT`。
2. 双击根目录 **启动AI示例教学.bat**。
3. 页眉应显示“AI 示例论文教学助手”，页面提示正在编辑独立 AI 教学项目。
4. 点击左侧 **AI 示例教学**，按九项练习逐步操作。
5. 第一次先进入“检查与导出 → 生成 PDF”，不修改内容，确认本机能运行。

普通入口 `启动论文助手.bat` 操作 `markdown/thesis/`。
教学入口操作 `markdown/examples/ai/thesis/`，生成结果位于 `markdown/build-ai/thesis.pdf`。
两者的文件、历史版本和 PDF 分开存放。请勿把教学目录直接覆盖到自己论文上。

## 九项教学实验

| 步骤 | 操作 | 如何判断完成 |
|---|---|---|
| 1 整篇编译 | 保持默认资料，生成 PDF | 能找到封面、中英文摘要、目录、五章正文、参考文献和两个附录 |
| 2 封面信息 | 把张三改为“练习学生”，等自动保存，再编译 | 封面姓名改变；题目不变 |
| 3 正文修改 | 在绪论的一段后添加自己的练习句 | PDF 对应位置出现新句子；历史版本可恢复 |
| 4 图片和表格 | 在第二章改一处图题，用三线表按钮粘贴 Excel 两行两列 | 图题更新，新表有独立编号，正文引用能跳到正确编号 |
| 5 编号公式 | 在第三章通过公式按钮插入 `y = ax + b` | 新公式自动编号，随附引用没有问号；原公式仍存在 |
| 6 模拟数字 | 对照第五章计算 `(12.6 - 7.4) / 12.6 × 100%` | 得到约 41.3%；只验证算术，不将其解释为真实测量 |
| 7 文献引用 | 在绪论点击文献引用，选择已有条目，再编译 | 正文出现引用编号，参考文献中有对应条目；这不代表文献已核验 |
| 8 章节管理 | 新增“拓展实验”，上移，移出，再恢复 | 目录随重新编译更新；移出的文件保留且能恢复 |
| 9 代码附录 | 查看附录一，修改一行注释再编译 | 代码排版保留缩进，注释变化可见；不代表代码经过硬件测试 |

编号会受图表插入位置和 LaTeX 浮动排版影响，不要求新增图片恰好出现在编辑位置的同一页。
修改章节结构后，绪论中“全文共分为五章”等描述需自行更新；自动目录不会替作者改写正文。
上传新图片会插入新对象，不会自动替换旧图片；若练习替换，请同时修改旧图的路径，或删除旧对象并调整对应引用。

## 示例内容在哪里

| 内容 | 教学项目文件 |
|---|---|
| 姓名、题目、关键词 | `metadata.yaml`（通常通过页面填写） |
| 绪论与引用 | `chapters/chapter1.md` |
| 系统架构、界面原型、指标表 | `chapters/chapter2.md` |
| 硬件图、电流换算公式、器件表 | `chapters/chapter3.md` |
| 软件流程、滤波与控制公式 | `chapters/chapter4.md` |
| 模拟曲线、响应时间、能耗对比 | `chapters/chapter5.md` |
| C 代码练习 | `appendices/appendixA.md` |
| 图片清单与插图代码 | `appendices/appendixB.md` |
| 原文献库 | `references.bib` |

以上路径相对于 `markdown/examples/ai/thesis/`。
保留的原始 ZIP 和原始预览 PDF 位于 `markdown/examples/ai/original/`。页面可直接打开原始预览。

## 本次转换做了什么

- 保留五章主体、摘要、总结、谢辞、两份附录、原图像文件及原文献库。
- 将章节、图表、公式和代码转换为助手支持的 Markdown 写法，保留原有对象标签与引用关系。
- 第二章原稿的“界面原型图”错误指向硬件图；教学改编版改为原包已有的 `fig_ui_mockup.png`，原始 ZIP 未改。
- 在中英文摘要前增加 AI 模拟内容说明；在第五章 AdamW 多行公式前说明它是排版练习，与本文照明控制方法无关。
- Markdown 表格、代码块及图片浮动规则与原手写 LaTeX 不完全相同，因此不保证相同页数或逐页完全一致。
- 修复了分章转换时同名小节的重复标签、LaTeX 代码块语言选项转义和附录 PDF 目录链接目标。这些修复只应用于助手生成的工程，原始 LaTeX ZIP 未改。

## 这篇示例不能证明什么

数据、图表与测试结果用于教学模拟，不能直接作为真实研究成果提交。姓名、学号等也是示例。
原文献库保留了 `liu2024-campuslight` 条目，其中学校为“某某大学”，这是明确的演示占位内容，不能当作已证实存在的论文引用。
其他条目也未在本次教学适配中逐条核验；正式使用须核对原文、出处、年份和引用的相关性。
原文涉及 AlphaGo、YOLO 的内容不自动构成照明控制系统的实验依据；是否保留应由实际研究问题决定。
本次只测试排版软件链路，没有搭建 STM32 硬件，也没有执行传感器、节能或稳定性实验。

学完后关闭教学助手，运行普通 `启动论文助手.bat`，以自己的真实设计、实验数据和经核验文献开始写作。

## 页面内九项练习的文字参考

每次只修改一项，等待内容同步，重新生成 PDF 后检查。完成一项后点击左侧“AI 示例教学”继续。

### 先生成整篇论文

1. 关闭其他论文助手，确认页眉写着“AI 示例论文教学助手”。先不要修改任何文字。
2. 点下方按钮进入“检查与导出”，点击“生成 PDF”，保持启动黑窗口打开，等待处理结束。
3. 成功后点击“打开生成的 PDF”。依次查看封面、摘要、目录和正文；点击目录标题，看看能否跳到相应页面。
4. 如果失败，先看页面给出的具体文件与行号。程序未找到时回“开始”刷新状态，确认完整解压了 runtime；不要连续重复点击编译。

### 修改封面信息

1. 点下方按钮进入“填写资料”，找到姓名字段，将“张三”改为“练习学生”。本次只改姓名，其他字段先不动。
2. 停止输入后等待页眉显示“内容已同步”。如果提示保存失败，先解决提示，不要关闭页面。
3. 点左侧“检查与导出”重新生成 PDF，再打开新结果；已经打开的旧 PDF 标签页需要刷新。
4. 封面应出现“练习学生”。随后可同样修改学号、专业和指导教师；示例信息不能用于正式提交。

### 修改一段正文

1. 点下方按钮打开“绪论”。# 开头的是章标题，## 是节标题，### 是小节标题；正文直接输入，不要给普通段落加 #。
2. 在第一段末尾另起一段，输入“这是一段用于学习排版的练习文字。”，按 Ctrl+S 保存。
3. 重新生成 PDF，在绪论中找到刚输入的句子。源文件中的空行用来分段，PDF 的字体和段落样式由模板设置。
4. 想撤销已保存的练习时，点“历史版本 / 恢复”，选择绪论和修改前的版本。恢复会改变当前内容，操作前确认选对文件与时间。

### 图片与表格

1. 进入第二章，找到 ![OLED/上位机监测界面原型图]。只改方括号内的图题，例如末尾加“（教学练习）”；圆括号里的路径和花括号里的标签先不要改。
2. 将光标放在完整段落后面的空行，点击“三线表”。标题填写“练习测量记录”。
3. 在 Excel 中准备两列：第一行“次数”和“读数”，第二行“1”和“10”。选中这四个单元格复制，粘贴到表格窗口，再点“确定”。不要用空格代替 Excel 的列分隔符。
4. 生成 PDF，核对新图题、新表题、三条横线及随表插入的引用。若提示列数不一致，重新复制完整矩形区域。想加新图片时在空行使用“插入图片”，它会新增图片，不会自动替换旧图。

### 编号公式

1. 进入第三章，把光标放在一段正文之后的空行，避免插入已有公式、表格或代码块内部。
2. 点击“编号公式”，保留 y = ax + b，直接点“确定”；输入框内不用额外写 $$。
3. 页面会插入公式块和“由式可知……”引用句。把省略号改为“这是用于学习编号的示例表达式。”，保留自动生成的标签和链接。
4. 生成 PDF，确认公式有编号、引用没有问号。出错时通过历史恢复本次插入，再使用最简单的 y = ax + b 重试。

### 模拟数据核对

1. 打开第五章，找到模拟能耗对比表，记录常亮方案 12.6 kWh 和智能控制方案 7.4 kWh。
2. 用计算器输入 (12.6 - 7.4) / 12.6 * 100，结果约为 41.3%。对照正文中的百分比是否一致。
3. 先不改这些数值。若练习修改数据，要同时检查正文、表格和图片：本项目中的曲线图片不会因修改表格自动重画。
4. 这里练习的是数字核对，不是进行真实实验。第五章保留的 AdamW 公式只展示多行公式排版，与照明控制算法无关。

### 文献引用与文献库

1. 先在绪论某个完整句子末尾点击，放置光标；然后点击工具栏“文献引用”，不用先编辑 references.bib。
2. 在“已有文献代号”下拉框中选择一条已有条目，再点“确定”。正文将出现 [@代号]，它是引用标记，生成 PDF 后才显示为文献编号。
3. 重新生成 PDF，检查正文引用和末尾参考文献。文献库中有条目并不代表它一定会出现在参考文献列表中，要看正文是否引用及模板设置。
4. 有真实来源时，可在同一下拉框选择“＋ 填写新文献”逐项填写，或“＋ 导入 BibTeX 文件”选择 .bib，导入后再选条目引用。不要编造作者、年份和 DOI。
5. 左侧“参考文献库”用于查看原始条目，通常不必手动改。@ 后面是类型，花括号后的第一个名称是代号；改代号后旧的正文引用也必须同步修改。原库含“某某大学”占位条目，仅供练习。

### 章节增减与恢复

1. 点下方按钮后，再点“管理章节（增减 / 排序）”。点击“＋ 新增一章”，名称填“拓展实验”，不用写“第六章”。
2. 新增后点“完成”，在新章标题下面写一句练习正文，再保存。重新生成 PDF，目录应多出这一章。
3. 再次打开章节管理，找到“拓展实验”，点“上移”。重新生成 PDF，检查顺序和编号变化；不要手动给标题编号。
4. 点“移出正文（保留文件）”，该章不再参与编译；在窗口下方可点“恢复到正文末尾”。至少要保留一章。
5. 五章不是限制。章节增减后，正文中“全文共分为五章”等文字不会自动改写；移出某章后，也要处理其他章节对其图表的引用。

### 代码附录与开始正式写作

1. 打开附录一，观察代码前后各有一行三个反引号；它们围住的内容会按代码块排版。
2. 第一次只修改代码块以外的一句解释文字，不改代码或反引号。保存并生成 PDF，检查附录中的解释是否更新。
3. 练习完成后关闭教学助手，双击普通“启动论文助手.bat”开始自己的论文。两种入口编辑不同目录，教学内容不会自动变成正式论文。
4. 需要保留练习时，关闭助手后复制整个 XIT 文件夹作为备份；不要覆盖自己已有的论文。历史版本是辅助恢复，不能代替独立备份。
