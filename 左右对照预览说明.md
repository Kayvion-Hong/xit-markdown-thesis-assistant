# 左右对照、连续阅读与双向定位

## 第一次使用

1. 完整解压，双击 `启动论文助手.bat`；教学演示可双击 `启动AI示例教学.bat`。
2. 点击“写论文”。顶部选择摘要、章节或参考文献。左侧 Markdown 编辑器铺满左栏；右侧是实际 LaTeX 编译的 PDF。
3. 点击一次“立即编译”。升级后旧 PDF 没有定位数据，需要重新编译后才能双向跳转。
4. 默认停止输入 3 秒后自动保存并编译。生成 PDF 需要时间，状态显示“已更新”后才是新结果；编译期间仍可继续写。

## 看整篇 PDF

- 鼠标放在右侧，用滚轮或触控板上下滚动，连续查看全部页面；不必逐页切换。
- 页码框可以输入页码并按回车，左右箭头仍可翻页；滚动时页码随当前主要可见页面更新。
- 缩放菜单提供适合宽度、适合整页、25%、50%、67%、75%、90%、100%、110%、125%、150%、175%、200%、250%、300%。
- 百分比框可输入 25–300 之间的自定义值，例如 137%；加减按钮每次调整 25 个百分点。100% 是 PDF 常规显示比例，不是“铺满右栏”。
- 较大比例时可横向滚动。页面按需绘制，滚到远处会释放已离开视野的画布。
- 如需单独阅读或下载，点击“单独打开 PDF”。

## 双击来回找位置

**左 → 右：**双击 Markdown 的某个段落、标题、图表语法或代码块，右侧会滚到对应 PDF 位置并短暂高亮。也可先把光标放到该处，点击“定位到 PDF”。

**右 → 左：**双击 PDF 的正文、图表附近或代码文字，左侧会自动切换到对应章节，并选中对应内容块。跨页段落/代码会结合文字内容寻找所属块。

这是“内容块/段落级定位”，不是字符级光标同步。图表可能浮动到下一页，定位依据 PDF 中实际生成的位置标记；空白处会采用附近标记。特殊自定义 LaTeX 或非常复杂的排版可能需要手动查找。

- 如果源文件已修改但没有成功编译，助手会提示先编译，不用旧 PDF 的位置覆盖新内容。
- 封面、目录、页眉页脚、自动生成的参考文献条目不是普通 Markdown 正文，不承诺双击到精确源行。封面请通过“返回功能菜单 → 填写资料”修改，文献请打开参考文献库。
- 高亮选中的是段落/内容块；直接输入会替换选中内容。若只想改几个字，先点击段落内相应位置。

## 用 AI 示例练习

1. 打开 AI 示例教学，进入“写论文”，立即编译一次。
2. 选择“第 2 章：系统总体设计”，双击开头段落，查看右侧对应页面。
3. 在 PDF 中双击这段文字，观察左侧选中范围。
4. 修改一处文字，等待自动编译状态变为“已更新”，再次定位比较。
5. 把缩放改成 50%，滚动查看相邻两页；试用页码框跳到附录，双击代码回到源文件。
6. 用 Ctrl+Z 撤销练习修改；停止输入后会自动保存和重新编译。

AI 示例仅用于教学，其中的数据和文献不能作为已核验的研究成果提交。

## 保存、报错与离线

原有自动保存、本地草稿、撤销重做、历史版本和整篇备份保留。编译失败会保留上一次成功 PDF 及其定位数据，并显示失败提示。最新全文日志保存在 `markdown/.preview/thesis/last-logs`，AI 教学对应 `markdown/.preview/ai/last-logs`。

运行环境和 PDF 预览资源随包提供，可离线使用；自行新增未包含宏包时可能仍需联网补装。中文安装路径会使用英文临时工作目录完成编译，源文件仍留在原位置。

这是一套本机 Markdown 写作和 LaTeX 预览工具，不提供 Overleaf 的多人协作或云端存储。升级新 ZIP 请解压到新目录并保留旧论文及备份；不要用示例文件直接覆盖已写内容。
