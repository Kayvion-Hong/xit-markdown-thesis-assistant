# 自动保存、意外关闭恢复和离线使用

## 平时不用反复点击保存
正文停止输入约 1.2 秒后保存到本机论文文件，基本资料约 1.6 秒后保存。确认“内容已同步”最稳妥。页面里的“关闭助手”会先尝试保存。

## 新增浏览器草稿保护
每次正文输入、撤销、辅助插入及资料表单修改时，同步保存一份浏览器本地草稿，补上等待自动保存期间的保护。成功写入论文文件后，清除内容一致的草稿；较新的草稿不会被旧保存请求删除。

意外关闭后：
1. 保持项目文件夹原位置，用同一个浏览器、同一个用户配置启动同一入口。
2. 页面会提示“发现浏览器草稿”，点击“查看 / 恢复”。
3. 核对时间与内容，可先下载草稿副本，再点击“恢复这份草稿”。
4. 恢复成功会保存到论文文件。需要看排版时重新生成 PDF。

普通项目与教学项目分开，多个页面的草稿也分开。恢复不是悄悄覆盖磁盘文件，避免把旧草稿写到新版本上。已移出正文的章节需先通过章节管理恢复后再恢复其草稿。

## 可以离线运行吗
Windows 完整包已内置 Python、依赖库、Pandoc、XeLaTeX 和 Biber。界面资源均为随包文件，核心编辑、保存、备份、恢复及现有示例 PDF 编译不调用云端服务。
条件：完整解压、运行环境未损坏、Windows 已有模板所需字体。打开外部网页、下载更新、补装缺失组件、添加随包没有的宏包仍可能需要网络。AI 示例是静态教学内容，不是联网 AI 生成服务。
本次检查为源代码依赖核查及本地运行测试，没有修改系统网络设置，也没有声称做了断网虚拟机测试。

## 使用边界
- 浏览器草稿不是云端同步；换电脑、浏览器、用户配置或无痕窗口，不能保证找到它。
- 默认按项目路径固定本机端口；不要在未恢复草稿前移动项目文件夹或手动改端口。移动路径会改变项目身份和默认地址。
- 端口被占用时会明确报错，请关闭旧助手；不会悄悄换地址导致草稿不可见。
- 清除网站数据、浏览器存储不足、损坏、断电或磁盘故障仍可能丢失内容。草稿保护失败时页面会持续提示；不能承诺“任何情况下绝不丢字”。
- Ctrl+Z 撤销步骤仍只保留在页面会话内；草稿恢复的是文字内容，不是整套撤销步骤，也不包括尚未提交的弹窗表单。
- 不要同时启动多个程序编辑同一项目。重要阶段仍保存整篇备份，并另存其他磁盘。
