# Windows 兼容性与环境修复

## 0.4.9 中文路径编译修复

论文和 ZIP 可以继续放在中文目录。随包 TeX 编译时自动使用英文临时工作目录和指向原运行环境的临时目录联接，完成后把 PDF 和编译产物写回原输出目录；不搬动、不修改论文源文件，不需要联网下载或修改系统 PATH。临时联接清理时只移除联接本身。

右侧预览的完整日志保存在 `markdown/.preview/thesis/last-logs`（AI 示例为 `markdown/.preview/ai/last-logs`），失败后也能继续查看。首次运行 Biber 可能需要较长时间，之后复用临时缓存。

若 Windows 限制创建目录联接、临时目录不可写，或无法找到英文临时路径，仍会显示错误；不能保证兼容所有机房策略。升级时请关闭旧助手，完整解压新版到新目录，保留旧版和论文备份。


## 开始使用
完整解压到短路径（推荐 D:\XIT），双击启动 BAT。不要在压缩包内运行，也不要把 ZIP 再解压在多层同名文件夹中。完整包的运行环境免手动配置 PATH，通常不需要管理员权限。

启动会检查 64 位环境、Windows 版本、关键程序是否完整、目录是否可写，并提示过长路径。网页“开始 → 运行诊断”可查看架构、最长路径、磁盘空间和缺失文件。

## 架构边界
主要目标为 Windows 10/11 x64。32 位系统和旧版 Windows 不属于当前完整包支持范围。Windows 11 ARM 支持 x64 仿真，可以尝试，但本项目没有 ARM 实机验证，不能宣称是 ARM 原生安装包。Windows 10 ARM 不支持同样的 x64 仿真能力。
依据：https://learn.microsoft.com/en-us/windows/arm/apps-on-arm-x86-emulation

## 缺少字体也可以先写作
“检查与导出 → 用随包字体生成预览 PDF”使用随包的 Fandol、TeX Gyre Termes、Latin Modern。默认生成在原字体文件缺失时也采用这一模式，并在日志、报告和 PDF 打开按钮上提示替代字体。
这些字体不会安装进 Windows，不需要下载字体。替代字体的字形、字宽和分页可能不同，不能宣称与原模板宋体、黑体、Times New Roman 完全一致。正式提交前以学校要求核验；原字体完整时普通“生成 PDF”继续使用原模板字体。
Windows 字体没有直接复制进公开 ZIP，相关再分发限制见：https://learn.microsoft.com/en-us/typography/fonts/font-faq
字体来源与许可材料见 third-party-fonts/；原字体文件仍由权利人许可决定。

## 宏包按需补装
现有示例需要的宏包已随包提供。自行加了新 LaTeX 命令而缺包时：
1. 先看首个错误里的缺失文件名，如 xxx.sty。
2. 在 CTAN 查明它所属的 TeX Live 宏包名称；文件名与包名不一定相同。
3. “开始 → 联网补装 TeX 宏包”，输入单个包名，如 siunitx，确定后才联网安装。
4. 等日志完成，再重新生成 PDF。
安装只使用本项目 runtime 下的 tlmgr，不修改系统 TeX。不要输入命令或多个包名。网络失败、组织代理限制或 TeX Live 年份不匹配时会显示原始日志；不要盲目跨年份升级。这个入口不是预装所有 TeX Live 包，也不能保证任意宏包组合兼容。
官方说明：https://tug.org/texlive/tlmgr.html

## 路径、权限、安全软件
- 路径过长：开始写作前换到 D:\XIT；已有浏览器草稿先恢复保存再搬迁，避免地址变化找不到草稿。
- 目录不可写：改用本人有权限的目录；学校机房请联系管理员。
- 程序缺失或被拦截：核对 ZIP 完整性、查看安全中心保护历史；不要关闭杀毒软件或绕过学校策略。
- 不自动修改注册表、系统长路径策略或全局 PATH；这些限制无法靠一个 ZIP 在所有电脑上消除。

## 验证范围
在本机 Windows 11 x64 完成原字体和随包字体实际编译；替代字体 PDF 共 29 页，已检查嵌入字体不含 SimSun、SimHei、Times New Roman、Consolas。26 项 Python 测试通过。ARM、Windows 10 实机、机房权限和不同安全软件未做覆盖测试。
